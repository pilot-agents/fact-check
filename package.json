{
  "name": "@pilot-agents/fact-check-mcp",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "version": "0.1.0",
  "description": "MCP server that forces an AI agent to actually verify claims: quotes are matched against fetched evidence, coverage is computed from the source text, and the final report is generated by the tool - not reported by the agent.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "fact-check",
    "fact-checking",
    "verification",
    "citation",
    "evidence",
    "ai-agent"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pilot-agents/fact-check.git"
  },
  "bugs": {
    "url": "https://github.com/pilot-agents/fact-check/issues"
  },
  "homepage": "https://github.com/pilot-agents/fact-check#readme",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "fact-check-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsx scripts/clean-dist.ts && tsc -p tsconfig.build.json && tsx scripts/make-bin-executable.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "check:leaks": "tsx scripts/leak-check/main.ts",
    "verify": "pnpm lint && pnpm test && pnpm build && pnpm check:leaks && pnpm e2e && pnpm e2e:package",
    "test": "vitest run",
    "e2e": "pnpm build && tsx e2e/run-e2e.ts",
    "e2e:package": "pnpm build && tsx e2e/run-package-e2e.ts",
    "start": "tsx src/index.ts",
    "viewer": "tsx src/viewer/main.ts",
    "report:rebuild": "tsx src/report/rebuild-cli.ts",
    "prepublishOnly": "pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm check:leaks --require-denylist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "cheerio": "^1.2.0",
    "pdfjs-dist": "^6.3.289",
    "playwright": "^1.63.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13",
    "@types/node": "^22.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  }
}
